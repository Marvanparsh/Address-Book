# Show only important logs in Production (minimized verbosity)
logging.level.root=WARN
logging.level.com.example.addressbook=WARN

# Log to a file (no console logs in Production)
logging.file.name=/var/logs/addressbook-prod.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
